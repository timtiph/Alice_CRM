<form method="post" action="{{ path('app_serp_result_delete', {'id': serp_result.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ serp_result.id) }}">
    <button class="btn">Delete</button>
</form>
