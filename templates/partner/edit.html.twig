{% extends 'base.html.twig' %}

{% block title %}Modifier le partenariat | Alice CRM{% endblock %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h1>Modification du partenariat : {{ partner.name }}</h1>
                <div class="button-go-back" alt="Retour page précédente" onclick="window.history.back();">
            <i class="fa-sharp fa-solid fa-circle-chevron-left fa-2xl"></i>
        </div>
    </div>


        <div class="d-flex justify-content-center justify-content-lg-end align-items-center">
        <a href="{{ path('app_partner_list') }}" class="btn btn-alice mx-2">Retour à la liste</a>
    </div>

    <div class="form mt-3">
        {{ include('partner/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>

{% endblock %}
