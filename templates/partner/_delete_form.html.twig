<form method="post" action="{{ path('app_partner_delete', {'id': partner.id}) }}" onsubmit="return confirm(' Êtes-vous sûr de vouloir supprimer ce partenariat ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partner.id) }}">
    <button class="btn btn-alice-supp">Supprimer</button>
</form>
