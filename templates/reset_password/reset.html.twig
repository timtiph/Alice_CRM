{% extends 'base.html.twig' %}

{% block title %}Réinitialisez votre mot de passe | Alice CRM{% endblock %}

{% block body %}

    

    <div class="d-flex align-items-center justify-content-between mb-5">
        <h1>Réinitialiser votre mot de passe</h1>
                <img src="{{ asset('assets/img/VectorBack.png') }}" class="goBack" id="goBackBtn" alt="Retour" onclick="window.history.back();"></img>
    </div>

    <div class="d-flex justify-content-center mt-3"></div>
        {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword) }}
            <button class="btn btn-alice-form">Réinitialisez votre mot de passe</button>
        {{ form_end(resetForm) }}

{% endblock %}
