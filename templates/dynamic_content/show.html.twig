{% extends 'base.html.twig' %}

{% block title %}DynamicContent{% endblock %}

{% block body %}
    <h1>DynamicContent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dynamic_content.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dynamic_content.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ dynamic_content.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dynamic_content_index') }}">back to list</a>

    <a href="{{ path('app_dynamic_content_edit', {'id': dynamic_content.id}) }}">edit</a>

    {{ include('dynamic_content/_delete_form.html.twig') }}
{% endblock %}
