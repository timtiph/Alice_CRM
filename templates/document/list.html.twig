{% extends 'base.html.twig' %}

{% block title %}Documents | Alice CRM{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-5">
        <h1>Documents</h1>
        <img src="{{ asset('assets/img/VectorBack.png') }}" class="goBack" alt="Retour" onclick="window.history.back();"></img>
    </div>

    <div class="d-flex justify-content-center justify-content-lg-end">
        <a href="{{ path('app_document_new') }}" class="btn btn-alice-lg">Nouveau document</a>
    </div>

    <table class="table">
        <thead>

            <tr>
                {# sorting of properties based on query components #}
                {% if is_granted('ROLE_ADMIN') %}
                    <th class="d-none d-sm-table-cell" style="width: 10%;">User</th>
                {% else %}
                {% endif %}

                <th class="" style="width: 10%;"{% if pagination.isSorted('d.name') %} class="sorted"{% endif %}>
                {{ knp_pagination_sortable(pagination, 'Titre', 'd.name') }}
                </th>
                
                <th class="d-none d-sm-table-cell" style="width: 20%;">Description</th>
                
                <th class="" style="width: 10%;">
                {{ knp_pagination_sortable(pagination, 'Date', 'd.date') }}
                </th>

                <th class="" style="width: 10%;" {% if pagination.isSorted('d.family') %} class="sorted"{% endif %}>
                {{ knp_pagination_sortable(pagination, 'Famille', 'd.family') }}
                </th>

                <th class="" style="width: 10%;">Actions</th>
            </tr>

        </thead>
        <tbody>
        {% for document in pagination %}
            <tr>
                {% if is_granted('ROLE_ADMIN') %}
                    <td style="width: 20%;">
                        <div class="">
                            {% for userElement in document.user.toArray() %}
                                <div class="form-check">
                                    <input class="form-check-input" disabled type="checkbox" id="user_{{ userElement.id }}" name="document[user][]" value="{{ userElement.id }}" {% if document.user.contains(userElement) %}checked{% endif %}>
                                    <label class="form-check-label" for="user_{{ userElement.id }}">
                                    {{ userElement.lastname|capitalize }} {{ userElement.firstname|upper }} | {{ userElement.role|upper }}
                                    </label>
                                </div>
                            {% endfor %}
                        </div>
                    </td>
                {% endif %}
                <td style="width: 10%;">{{ document.name }}</td>
                <td style="width: 20%;" class="d-none d-sm-table-cell">{{ document.description }}</td>
                <td style="width: 10%;" class="">{{ document.date ? document.date|date('d/m/Y') : '' }}</td>
                <td style="width: 10%;" class="">{{ document.family }}</td>
                <td style="width: 10%;">
                    <div class="d-flex ">
                        <a href="{{ path('app_document_show', {'id': document.id}) }}" class="buttonShowDocument m-0 mx-2"><img src="{{ asset('assets/img/eye-solid.svg') }}" alt="Voir"></a>

                        <a href="{{ asset('uploads/documents/' ~ document.fileName) }}" class="buttonDownloadDocument m-0 mx-2" target="_blank" rel="noopener noreferrer" download><img src="{{ asset('assets/img/download-solid.svg') }}" alt="Télécharger" style="height: 23px;"></a>

                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">Aucun document</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <!-- afficher les liens de pagination -->
    <div class="">
        {{ knp_pagination_render(pagination) }}
    </div>


{% endblock %}
